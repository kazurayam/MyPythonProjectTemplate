[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pytest = "*"

[dev-packages]
autopep8 = "*"

[scripts]
test = "python -m pytest"
format = "autopep8 -ivr ."

[requires]
python_version = "3.8"
